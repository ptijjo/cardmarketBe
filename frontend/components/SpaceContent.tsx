

const SpaceContent = ({children}:{children:React.ReactNode}) => {
  return (
    <div className="mx-auto w-screen max-w-5xl flex items-center justify-between">
      {children}
    </div>
  )
}

export default SpaceContent
